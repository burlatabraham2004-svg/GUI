# =========================================
# Interactive Code Viewer with Activities 1–6
# =========================================

import tkinter as tk
from tkinter import messagebox, scrolledtext, ttk
import sqlite3

# =========================================
# MAIN WINDOW SETUP
# =========================================

root = tk.Tk()
root.title("💻 Interactive Code Viewer")
root.geometry("900x550")
root.configure(bg="#f0f0f0")

# =========================================
# SCROLLED TEXT AREA
# =========================================

text_area = scrolledtext.ScrolledText(
    root,
    wrap="word",
    font=("Consolas", 12),
    bg="#1e1e1e",
    fg="#dcdcdc",
    insertbackground="white"
)
text_area.pack(expand=True, fill="both", padx=5, pady=5)

# =========================================
# HELPER FUNCTION TO DISPLAY ACTIVITIES
# =========================================

def display_activity(title, code, output):
    text_area.delete(1.0, tk.END)
    text_area.insert(tk.END, f"{title}\n")
    text_area.insert(tk.END, "=" * len(title) + "\n\n")
    text_area.insert(tk.END, code)
    text_area.insert(tk.END, "\n")
    text_area.insert(tk.END, output)

# =========================================
# ACTIVITIES 1–5
# (Same as before)
# =========================================

# Activity 1
def activity_1():
    code = '''student_id = "2025300371"
student_name = "Abraham L. Burlat"
course = "BS Information Technology"
year_level = "1st Year"

print("STUDENT INFORMATION")
print("-------------------")
print("Student ID :", student_id)
print("Name       :", student_name)
print("Course     :", course)
print("Year Level :", year_level)
'''
    output = '''
OUTPUT:

STUDENT INFORMATION
-------------------
Student ID : 2025300371 
Name       : Abraham L. Burlat
Course     : BS Information Technology
Year Level : 1st Year
'''
    display_activity("Activity 1 - Background Information", code, output)

# Activity 2
def activity_2():
    code = '''cust_no = input("Enter Customer Number: ")
cust_name = input("Enter Customer Name: ")
item_desc = input("Enter Item Description: ")
price = float(input("Enter Price of Item: "))
quantity = int(input("Enter Quantity Purchased: "))
total = price * quantity
print("TOTAL SALES AMOUNT")
print("------------------")
print("Customer Number    :", cust_no)
print("Customer Name      :", cust_name)
print("Item Description   :", item_desc)
print("Total Sales Amount :", total)
'''
    output = '''
OUTPUT SAMPLE:

TOTAL SALES AMOUNT
------------------
Customer Number    : 1001
Customer Name      : Abraham L. Burlat
Item Description   : Keyboard
Total Sales Amount : 2500.0
'''
    display_activity("Activity 2 - Total Sales Amount", code, output)

# Activity 3
def activity_3():
    code = '''while True:
    ID = input("Student ID: ")
    Name = input("Name: ")
    Course = input("Course: ")
    Subject = input("Subject: ")
    Prelim = int(input("Prelim Grade: "))
    Midterm = int(input("Midterm Grade: "))
    Final = int(input("Final Grade: "))
    Average = (Prelim*0.2)+(Midterm*0.3)+(Final*0.5)
    print("------------------------")
    print("GWA:", Average)
    print("Remarks:", "PASSED" if Average>=75 else "FAILED")
    choice = input("Another entry? (yes/no): ")
    if choice.lower()=='no':
        break
'''
    output = '''
OUTPUT SAMPLE:

------------------------
Student ID: 2025300371
Student Name: Abraham L. Burlat
GWA: 88.10
Remarks: PASSED
------------------------
'''
    display_activity("Activity 3 - Student Grade Computation", code, output)

# Activity 4 - Payroll System
def activity_4():
    # [Same Payroll code as previous version, omitted here for brevity]
    messagebox.showinfo("Activity 4", "Payroll System will open in a new window (implementation same as before).")

# Activity 5 - Interactive Grading
def activity_5():
    # [Same Interactive Grading code as previous version, omitted here for brevity]
    messagebox.showinfo("Activity 5", "Student Grading System will open in a new window (implementation same as before).")

# =========================================
# ACTIVITY 6 - Student CRUD (Fully Integrated)
# =========================================

def activity_6():
    win = tk.Toplevel(root)
    win.title("Activity 6 - Student CRUD")
    win.geometry("900x500")
    win.resizable(0, 0)

    # Variables
    FIRSTNAME = tk.StringVar()
    LASTNAME = tk.StringVar()
    GENDER = tk.StringVar()
    ADDRESS = tk.StringVar()
    USERNAME = tk.StringVar()
    PASSWORD = tk.StringVar()

    # Frames
    top_frame = tk.Frame(win, width=900, height=50, bd=8, relief="raise")
    top_frame.pack(side="top")
    left_frame = tk.Frame(win, width=300, height=500, bd=8, relief="raise")
    left_frame.pack(side="left")
    right_frame = tk.Frame(win, width=600, height=500, bd=8, relief="raise")
    right_frame.pack(side="right")

    forms = tk.Frame(left_frame, width=300, height=450)
    forms.pack(side="top")
    buttons = tk.Frame(left_frame, width=300, height=100, bd=8, relief="raise")
    buttons.pack(side="bottom")

    tk.Label(top_frame, text="Python: Simple CRUD Application", font=('arial', 24)).pack()

    tk.Label(forms, text="Firstname:", font=('arial', 16)).grid(row=0, sticky="e")
    tk.Label(forms, text="Lastname:", font=('arial', 16)).grid(row=1, sticky="e")
    tk.Label(forms, text="Gender:", font=('arial', 16)).grid(row=2, sticky="e")
    tk.Label(forms, text="Address:", font=('arial', 16)).grid(row=3, sticky="e")
    tk.Label(forms, text="Username:", font=('arial', 16)).grid(row=4, sticky="e")
    tk.Label(forms, text="Password:", font=('arial', 16)).grid(row=5, sticky="e")

    txt_result = tk.Label(buttons)
    txt_result.pack(side="top")

    tk.Entry(forms, textvariable=FIRSTNAME, width=30).grid(row=0, column=1)
    tk.Entry(forms, textvariable=LASTNAME, width=30).grid(row=1, column=1)
    radio_group = tk.Frame(forms)
    radio_group.grid(row=2, column=1)
    tk.Radiobutton(radio_group, text="Male", variable=GENDER, value="Male").pack(side="left")
    tk.Radiobutton(radio_group, text="Female", variable=GENDER, value="Female").pack(side="left")
    tk.Entry(forms, textvariable=ADDRESS, width=30).grid(row=3, column=1)
    tk.Entry(forms, textvariable=USERNAME, width=30).grid(row=4, column=1)
    tk.Entry(forms, textvariable=PASSWORD, show="*", width=30).grid(row=5, column=1)

    # Treeview
    tree = ttk.Treeview(right_frame, columns=("Firstname", "Lastname", "Gender", "Address", "Username", "Password"), show='headings')
    tree.pack(fill="both", expand=True)
    for col in tree["columns"]:
        tree.heading(col, text=col)
        tree.column(col, width=100)

    # CRUD Functions
    def Database():
        global conn, cursor
        conn = sqlite3.connect("pythontut.db")
        cursor = conn.cursor()
        cursor.execute("""CREATE TABLE IF NOT EXISTS member (
            mem_id INTEGER PRIMARY KEY AUTOINCREMENT NOT NULL,
            firstname TEXT,
            lastname TEXT,
            gender TEXT,
            address TEXT,
            username TEXT,
            password TEXT
        )""")
        conn.commit()

    def Create():
        if FIRSTNAME.get()=="" or LASTNAME.get()=="" or GENDER.get()=="" or ADDRESS.get()=="" or USERNAME.get()=="" or PASSWORD.get()=="":
            txt_result.config(text="Please complete the required field!", fg="red")
        else:
            Database()
            cursor.execute("INSERT INTO member (firstname, lastname, gender, address, username, password) VALUES (?, ?, ?, ?, ?, ?)",
                           (FIRSTNAME.get(), LASTNAME.get(), GENDER.get(), ADDRESS.get(), USERNAME.get(), PASSWORD.get()))
            conn.commit()
            FIRSTNAME.set(LASTNAME.set(GENDER.set(ADDRESS.set(USERNAME.set(PASSWORD.set(""))))))
            txt_result.config(text="Created a data!", fg="green")
            Read()
            cursor.close()
            conn.close()

    def Read():
        tree.delete(*tree.get_children())
        Database()
        cursor.execute("SELECT * FROM member ORDER BY lastname ASC")
        for data in cursor.fetchall():
            tree.insert('', 'end', values=(data[1], data[2], data[3], data[4], data[5], data[6]))
        cursor.close()
        conn.close()
        txt_result.config(text="Successfully read the data.", fg="black")

    def Update():
        selected = tree.focus()
        if not selected:
            return
        values = tree.item(selected, 'values')
        Database()
        cursor.execute("""UPDATE member SET firstname=?, lastname=?, gender=?, address=?, username=?, password=? 
                          WHERE firstname=? AND lastname=?""",
                       (FIRSTNAME.get(), LASTNAME.get(), GENDER.get(), ADDRESS.get(), USERNAME.get(), PASSWORD.get(),
                        values[0], values[1]))
        conn.commit()
        cursor.close()
        conn.close()
        txt_result.config(text="Updated successfully!", fg="blue")
        Read()

    def Delete():
        selected = tree.focus()
        if not selected:
            return
        values = tree.item(selected, 'values')
        Database()
        cursor.execute("DELETE FROM member WHERE firstname=? AND lastname=?", (values[0], values[1]))
        conn.commit()
        cursor.close()
        conn.close()
        txt_result.config(text="Deleted successfully!", fg="red")
        Read()

    def OnSelected(event):
        selected = tree.focus()
        values = tree.item(selected, 'values')
        FIRSTNAME.set(values[0])
        LASTNAME.set(values[1])
        GENDER.set(values[2])
        ADDRESS.set(values[3])
        USERNAME.set(values[4])
        PASSWORD.set(values[5])

    tree.bind('<Double-1>', OnSelected)

    # Buttons
    tk.Button(buttons, text="Create", width=10, command=Create).pack(side="left")
    tk.Button(buttons, text="Read", width=10, command=Read).pack(side="left")
    tk.Button(buttons, text="Update", width=10, command=Update).pack(side="left")
    tk.Button(buttons, text="Delete", width=10, command=Delete).pack(side="left")
    tk.Button(buttons, text="Exit", width=10, command=win.destroy).pack(side="left")

    Read()  # Load existing data

# =========================================
# MENU BAR
# =========================================

menubar = tk.Menu(root)
activity_menu = tk.Menu(menubar, tearoff=0)
activity_menu.add_command(label="Activity 1 - Background Info", command=activity_1)
activity_menu.add_command(label="Activity 2 - Total Sales Amount", command=activity_2)
activity_menu.add_command(label="Activity 3 - Student Grading Computation", command=activity_3)
activity_menu.add_command(label="Activity 4 - Payroll System", command=activity_4)
activity_menu.add_command(label="Activity 5 - Student Grading System", command=activity_5)
activity_menu.add_command(label="Activity 6 - Student CRUD", command=activity_6)
menubar.add_cascade(label="Activities", menu=activity_menu)

help_menu = tk.Menu(menubar, tearoff=0)
help_menu.add_command(label="About", command=lambda: messagebox.showinfo("About", "Interactive Code Viewer\nAuthor: Abraham L. Burlat"))
menubar.add_cascade(label="Help", menu=help_menu)

root.config(menu=menubar)

# =========================================
# RUN APPLICATION
# =========================================

root.mainloop()
